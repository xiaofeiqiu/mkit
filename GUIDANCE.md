# 项目 Guidance：游戏素材制作与集成规范

更新：2026-09-06。适用于 Whispering Forest 的所有新制及返修素材，是本项目美术生产的统一要求。制作、替换或集成素材前先读本文；历史生成提示词、样件与旧导出脚本不代表新规范。用户后续明确修改要求时，同步更新本文和共享导出配置。

**核心要求：所有有体积的场景素材先建立可编辑的三维模型，再共用一套固定的正交相机、世界比例、光照和颜色设置，输出透明二维素材。城市由独立素材布置，不生成整座城市背景图。**

本文定义制作与验收标准，不表示当前素材已全部符合标准或已完成三维重建。现有资产的过渡方案与实际状态见[场景素材统一投影标准](<game/whispering_forest/art/city/Projection Standard.md>)；完成情况必须以源文件、运行时引用和实机验证记录为准。

## 1. 目标风格

- **用户最新确认的方向（2026-09-06）**：怀旧、日漫感的 fantasy 城市。新一轮城市资产先生成概念图交用户评估，得到明确反馈确认后再开始对应三维建模；先逐件做好不同房屋、城墙、城门和树木，再按规划布置，不能用整城大模型或整城背景图替代独立资产。
- 明亮、清爽的复古斜视 RPG；使用用户提供的截图作为体块、颜色、细节密度与空间关系参考，不直接裁取其中素材。
- 2D 输出必须有清楚的体积：可分辨顶面、受光面、背光面、厚度、凹陷与接地关系。
- 草地保持明亮的自然绿色，避免整屏偏黄、橄榄绿滤镜、荧光绿、油亮叶片与塑料质感。阴影较冷，不能把全城压暗来制造立体感。
- 使用清楚的轮廓和有节制的纹理。最终游戏倍率下的可读性优先，不能靠放大后的细碎瓦片、叶片和噪点冒充质量。
- 建筑、树木、人物、地面及道具共用相近的色彩范围、边缘处理与细节密度；不能混用写实高清植物和简洁手绘建筑。

## 2. 固定投影与共享渲染配置

| 项目 | 强制标准 |
| --- | --- |
| 投影 | 正交投影；禁止透视相机及随距离缩小 |
| 地面 | 当前地图的 2:1 斜视投影，两组地面边线在屏幕上斜率为 `+0.5`、`−0.5` |
| 相机 | 相对地平面俯视 30°，相对建筑地面轴方位 45°，竖直方向为世界 Y 轴；不混入俯视 35.264° 的数学等轴测 |
| 世界坐标 | 三维 X/Z 为地面、Y 向上；逻辑地图沿用 `scripts/iso.gd` 的投影关系 |
| 方向变体 | 只旋转三维模型，保持相机和灯光不动；建筑制作四个地面轴向，玩家和战斗角色制作八个方向；静态 NPC 按摆放朝向导出 |
| 比例 | 所有模型共用世界单位与每单位像素数；禁止为了填满画布逐件自动缩放 |
| 输出 | 共享渲染器、采样、曝光、颜色处理与透明规则；每份输出记录同一套配置的版本与哈希 |

共享配置必须记录：相机投影类型、位置/观察目标或等价姿态、正交视野、画布尺寸、世界单位、像素密度、灯光方向/强度/颜色、环境光、曝光、色彩转换、抗锯齿、阴影和透明输出设置。所有导出器读取该配置，不能各自复制一组近似参数。

当前共享实现为 `game/whispering_forest/art/city/render_config.gd` 和 `render_stage.gd`。人物正式导出入口为 `tools/bake_world_characters.gd`，读取同一配置并在 `assets/characters/world-motion/manifest.json` 记录配置哈希、源文件、动作帧数、脚底原点及法杖杖头位置；旧 `bake_characters.gd` / `bake_performance.gd` 的历史输出不作为当前游戏人物素材入口。

地图关系是 `screen = (u − v, (u + v) / 2)`。对上述相机，可用以下关系检查模型投影；不包含画布原点偏移：

```text
p = 正交画面平面上每世界单位的像素数
s = p / sqrt(2)
u = s * world_x
v = s * world_z
screen_x = u - v
screen_y = (u + v) / 2 - p * cos(30°) * world_y
```

先用一个立方体、64×32 菱形地面格、标准人物和门洞标定共享配置。竖直高度也受相机俯角投影影响，不能直接把三维身高乘以 `p` 当作屏幕身高。当前人形约 72px，可用作首版标尺；选定世界身高后由上式求统一像素密度，不能单独拉伸角色或建筑来凑尺寸。

允许无缩放裁切透明边距，并保存裁切后的原点。大物件需要更大画布时，扩展取景范围并同比调整正交视野，维持像素密度、姿态和光照不变。高清烘焙可统一倍数降采样；不能给每个物件使用不同倍率。

禁止用整图旋转、水平镜像或非等比拉伸制造正式方向变体。镜像会改变建筑结构和受光方向；二维仿射校正无法修复生成图内部不同部位的透视冲突，只能标记为过渡资产。

## 3. 比例与统一光照

2026-09-06 明亮日光修订采用共享 `bellwake-daylight-v3` 配置。太阳照亮可见的左侧墙面，天空补光保留偏冷的背光层次；避免纹理原色与底色重复相乘压暗材质。城市场景通过独立模型投影遮罩表现树叶、屋檐与城垛的地面影子，运行时轻微软化边缘；自阴影仍随主体烘焙，地面投影只绘制一次。人物及立体法术跟随同一份配置重导出。

- 使用同一套人物、门洞、楼层、台阶、长椅、树木、墙体尺标。模型尺寸按世界单位定义，透明画布边距、屋顶纵深和树冠宽度不参与身高判断。
- 普通人形沿用统一身高基准；NPC、玩家、敌人的差异来自设计体型。普通门洞净高约为标准成人的 1.25–1.5 倍，普通楼层约 1.8–2.3 倍，作为首轮美术标定范围；大型公共建筑和城门另有明确尺寸设计。
- 树木根据树种和用途形成高度、冠幅梯度；路灯、椅面、门把、台阶必须与人物放在一起核对。禁止仅按 PNG 宽度随意设定显示比例。
- **主光固定来自屏幕左上。** 所有方向的屋顶、墙面、树冠和人物使用同一世界光源；背光在右下，地面投影朝右下。
- 受光顶面和左上侧较亮，背光面较暗且略冷；凹窗、檐下、砖缝和树冠内层有遮蔽关系。保留明亮的环境光，避免大面积死黑。
- 表面以哑光为主，叶片、瓦片、木材不能有不合风格的高光。共享灯光不意味着所有材质同色，材质自身的颜色与粗糙度仍有区别。
- 地面投影采用统一方法：可单独导出阴影层，也可由运行时统一绘制。每份资产注明是否自带投影，禁止重复叠加两份影子。模型自身阴影与接触遮蔽保留。

## 4. 建筑、城墙与材质

- 用户追加蓝瓦建筑近景作为屋顶/墙面材质参考：瓦片要有密实叠层、矿物颗粒和粗糙磨边，不能像平滑塑料板；墙面以浅色旧石材与有节制的灰泥变化协调。材质细节与几何搭接共同表现，保持各街区已定配色和共享日光。
- 2026-09-06 用户补充建筑结构参考：墙面、门窗、瓦片与接口需要真实进退深度；避免所有房屋都是规则长方体加统一屋顶。面包房采用挑出的楼层、橱窗与退进门厅，旅馆采用带支撑的阳台和较低附屋；其他功能建筑也按用途设计体块组合。墙洞有侧壁，瓦片有搭接厚度，四向均从同一模型导出，并保留入口和街道通行。

- 2026-09-06 用户再次指出房屋墙面太光滑、太新，要求按所给旧城资产板加强质感。本轮返修需在实际游戏倍率看清灰泥起伏、局部修补、剥落边缘、少量露石和自然使用痕迹；不能只加在正常倍率看不出的细噪点。保留明亮日光，主要通过表面材质与真实浅浮雕表现旧化。

- 同一建筑的四向来自同一个模型。门窗、烟囱、招牌、屋顶与后院位置必须对应，不能把四栋不同房子标成同一栋的四个方向。
- 建筑沿街道轴线、地块边界与临街线排布，入口连接步道；不能在屏幕上随意扭转房屋。公会、旅馆、铁匠铺、药草店、市场和住宅应有可辨识的轮廓和功能细节。
- 屋顶有脊、檐口厚度、瓦层和接缝；墙面有窗洞深度、窗台、门框、墙脚和材质转面。细节服从实际模型结构。
- 瓦片保持结构合理的铺设方向，但大小、搭接、修补色差和檐口轮廓应有克制的自然变化，避免机械复制般完全整齐。房屋不要做成崭新的塑料积木；临街宽度和进深应与楼层、门窗数量相称，避免两层住宅被挤成窄高盒子。
- 屋顶根据街区与建筑用途使用蓝灰、灰紫、陶红、灰绿等协调变体；不能全城一个颜色，也不能每栋随机使用冲突的高饱和颜色。
- 旧化局部、克制且有原因：檐下水痕、墙脚积灰、砖缝轻微苔色、少量补瓦、窗沿磨损。不能用全图脏污滤镜或均匀噪点代替材质。
- 城墙必须有厚度、墙基、砌块体积、灰缝、压顶/垛口及塔楼转接。适量磨损和污渍表现重量与使用痕迹，不做薄纸墙或崭新光滑积木墙。
- 城门连接连续墙体，门洞有实际深度，与桥、道路和碰撞开口对齐。墙段、转角、塔楼、门墙连接点使用同一网格与标高，不能只在总览中看起来接上。

## 5. 树木和绿化

- 参考用户最后确认的树图，以几组清楚的树冠体块表现叶簇。控制碎叶、枝条和树皮数量，游戏倍率下不出现密集闪烁的颗粒噪声。
- 树形有轻微偏冠、疏密变化、露枝、缺口与不完全对称的层次。不能所有树都是圆满、茂密、油亮、刚长成的标准形。
- 每类绿化准备不同轮廓或树龄变体，并区分街道阔叶树、庭院树、针叶树、灌木等。不能只换颜色冒充不同树种。
- 同一树冠具有左上亮、中间过渡、右下暗及内部空隙；不同树种允许灰绿、偏蓝绿等适度变化，但共用场景光照和细节密度。
- 树根有接地关系，树干和主要枝杈支撑树冠；避免悬浮树冠、无根落点或全图僵硬摆动。需要风动时，应表现枝叶局部运动并保持树根稳定。
- 按道路、院落、花园和入口规划种植，留出门口、视线、路灯和通行空间。规则树列也要有节制的自然差异，禁止在全城随机撒树。

## 6. 地面、路沿与城市细节

- 明确区分车行/主路铺砖、人行石板、路沿、草地与花池。路沿具有小幅真实高度、顶面、侧面和接触阴影，不能只画一条平面白线。
- 转弯使用合适的斜切角、圆角或折线过渡；不把所有路口、绿地和花坛都做成生硬直角。不是每个角都必须圆弧，应服从街区设计。
- 模块包含直段、内角、外角、斜切段、圆弧段、端头、入口退台与坡道。路面、路沿、人行道、花坛和树篱的转角轮廓必须对应，不能各走各的。
- 主街、宅前步道、门前台阶和公共前庭连通。铺砖方向遵循统一地面轴；适量破损、补缝、排水细节沿功能位置分布，不能铺满重复污点。
- 长椅、灯柱、花盆、告示牌、围栏和杂物服务于入口、等候、休憩或工作用途。细节要有位置理由，不为填满画面随意堆放。
- 低路沿、坡道与高花坛采用相应通行规则；可见入口、台阶、门洞和碰撞/导航一致。装饰不能挡住必经任务路线或传送落点。
- 建筑、树木、城墙及其他环境素材遮住角色时保持正常不透明度，不做遮挡渐隐；通过前后排序表现遮挡关系。素材背景仍使用正常的 RGBA 透明通道。

## 7. 人物与特效

- 玩家、NPC、敌人共用场景相机、比例和光照规范。角色八方向通过旋转同一模型得到，装备与持物的左右手保持一致。
- 玩家采用用户指定的日漫奇幻风格：鲜明的发束剪影、有神的眼睛和清楚的服装分区。发型用贴合头型、逐渐收尖的层次体块，避免粗圆条堆叠；正常移动保持视线稳定，腿部落脚与摆臂体现活力，不叠加高频全身震动。
- 用户已明确：城内 NPC 可以固定站立，不要求待机或移动动画。当前导师使用固定站姿；玩家和战斗敌人保留所需动作。
- 玩家持物为竖握的长法杖，杖尾靠近地面、杖头高于肩部。待机与行走保持竖持，施法表现抬杖、下压和收势；发射位置由模型的晶石杖头导出。
- 行走必须有真实动作序列：重心、落脚、摆臂及适度衣物滞后。八个静态朝向配整图上下漂移不算行走动画。
- 同一动作的所有帧共享地面原点，不逐帧自动居中。保存帧时长、方向、动作名、根节点和必要的动作事件；脚底不能因裁切变化而跳动。
- 用户最新要求：技能主动画统一升级到 **16 个独立制作的关键帧**，使用 Aseprite 保存可编辑图层、逐帧时长和阶段标签，先完成冰技能。迁移期间冰使用 16 帧，火、风、土和终极仍保留各自现有的 8 帧资产，不能将重复帧或插值帧计为制作完成。各阶段可以使用不同帧时长；冰体从同一模型、同一相机和同一画布导出，水纹、裂纹、高光和碎冰在 Aseprite 分层制作；禁止逐帧居中或缩放填满。
- 一级火焰按用户最新声音参考调整节奏：保留 8 帧，以新版完整爆音的实际时长（约 1.13 秒）作为动画终点，尾音与火焰一起淡出；音频不随机变速。此明确声音同步要求更新该技能原先的 2.4 秒时长。
- 一级冰改为水面向中心聚拢、中央蓄起水峰，随后固定大小的冰刺从地面以下快速刺出；取消整块冰先变大再回缩变小。成形冰体和碎片必须不透明；关闭冰技能的运行时透明淡出。尾段通过裂开、实心碎块移出和移除结束，不出现可透视人物或地形的冰体；背景透明与边缘抗锯齿保留。用户追加要求动作更快、冰为白色：冰体以霜白色为主，仅保留浅冷灰棱面阴影，去掉蓝色水柱感。16 帧时间轴缩短为 1.45 秒，0.24 秒开始破地；第 10 帧（索引 9）在 `ICE_PEAK=0.42` 秒到达最高姿态，伤害、冻结和用户指定的一级命中声音同时触发；聚水和破地上升阶段不提前扣血或播放命中音。
- 多重火、风、陨石、冰按释放队列依次创建，不在施法时一次性创建全部动画。默认从第一枚到最后一枚为 0.8 秒窗口，5 枚为 0/0.2/0.4/0.6/0.8 秒；窗口不包括单枚动画时长。每枚从实际释放起独立计算生长、命中、声音和淡出；速度强化只缩短释放窗口，不加速素材动画。
- 固定落点法术在释放时保存世界坐标，视觉与伤害共用这份坐标快照；冰柱生成后不跟随原目标的移动、击退、死亡或删除。移动龙卷拥有独立轨迹，不复用固定落点逻辑。
- 魔法素材进入运行时前，按迁移登记检查帧数（当前冰 16、其他主动画 8）、真实 alpha、画布边缘、模型原点、源文件/共享配置哈希；冰额外核对 Aseprite 源文件、16 张不同的合成帧、毫秒时长和峰值事件。`tools/spell_asset_checks.gd` 已接入战斗验证，并检查目标离开冰区与其他敌人进入冰区的实际伤害；仍需固定落点连续播放的美术验收。
- 有体积的落石、冰柱、召唤物和掉落物使用共享三维投影。贴地法阵、范围圈按地图投影，不能用屏幕正圆代替地面圆。
- 自发光的魔法可以有设计上的局部光色，但不能改变其他环境素材的默认光源。界面图标、字体与独立立绘不要求场景相机投影；平铺材质本身作为表面纹理，在模型或地面上验证方向与比例。

## 8. 标准生产流程

**当前城市重做的评估关口**：先交付建筑与环境模块概念板，供用户评估造型、比例、配色、旧化与日式幻想风格。在用户明确确认这一轮方向之前暂停继续新建或批量优化三维资产。已做的小样保留为开发记录，不标记为审美验收通过。

2026-09-06 本轮 A/B 概念板已由用户以“可以”“go”明确确认，现进入独立 Blender 资产制作和实机验证。此记录只确认概念方向，不预先宣称模型质量验收通过。

1. **登记需求。** 建立资产清单，写明用途、街区/章节、世界尺寸、朝向、动作、变体数量、材质、参考与预计使用位置。先确定轮廓与摆放逻辑。
2. **标定共享场景。** 使用固定正交相机、尺标、地面格和左上光源。把建筑、人物、树木和墙段放在同一校准场景，确定统一比例与细节密度。
3. **制作可编辑模型。** 先做正确的地基、厚度、门洞和体块，再做屋顶、窗框、树冠、旧化等细节；保存源模型、材质和生成脚本。AI 可辅助概念和材质，不能用独立生成图取代最终结构投影。
4. **完成小范围实机样段。** 至少包含一个街角、两种不同朝向建筑、一段墙及门连接、不同树形、人物和路沿转角。在实际游戏倍率验证比例、色彩、细节与光影；修好共性问题后再批量生产。
5. **统一批量渲染。** 所有方向/帧调用同一版本配置，只改变模型方向和动作。输出真实 RGBA 透明图，保存原点、裁切偏移、地基、门位、模块连接点与动画信息。
6. **做技术与视觉检查。** 检查投影、比例、透明通道、接缝、动画稳定性和实际倍率下的风格。后期不能通过自由重绘、镜像或非等比缩放破坏透视与光向；修改结构后重新渲染。
7. **实际集成。** 更新运行时目录、素材注册表/图集、场景引用、碰撞、排序和导航；重新导入并启动游戏。交付要求包括集成时，不能把素材放进审阅目录就停止。
8. **实机验收并记录。** 检查各方向、街区、门墙连接和人物遮挡，执行相关回归验证，保存真实引擎截图/录像和检查结果。失败项返修，不以生成成功或截图好看代替验收。

## 9. 交付文件与可追溯性

每套正式资产应有以下内容；已有目录结构可沿用，不能只保存最终 PNG：

| 内容 | 要求 |
| --- | --- |
| 可编辑源文件 | 模型/场景、材质、贴图、动作及程序生成脚本；能修改后重新导出 |
| 共享配置 | 相机、世界单位、光照、颜色与渲染参数，含版本与哈希 |
| 运行时素材 | 独立透明精灵或动画图集；使用稳定资产 ID 和明确朝向/动作命名 |
| 元数据 | 源文件、尺寸、像素密度、朝向、原点、裁切区域、地基/碰撞、门位、连接点、阴影方式、动画帧信息 |
| 生成记录 | 使用 AI 时记录提示词、实际参考输入、生成来源与被采用版本；使用外部素材时保留来源和许可 |
| 验收证据 | 检查命令与结果、真实引擎截图/录像、已知未完成项和实际加载位置 |

PNG 必须含真实 alpha。画在 RGB 图里的灰白棋盘格不算透明；在浅色、深色和彩色底上检查边缘，不能有底色框、白边、黑边或裁掉的屋檐与树冠。地基、门位和脚底锚点来自模型坐标，不能从图片最下面的可见像素猜测。

## 10. 完成标准与验收清单

资产状态明确区分：`概念/参考` → `模型完成` → `已导出` → `已集成` → `实机验收通过`。未满足下一状态条件时不能提前标记；改动过的资产需要重新验证相关项。

- [ ] 所有场景体积资产的导出记录指向同一共享配置版本，没有私自改相机或逐件缩放。
- [ ] 地基两轴为 ±0.5 斜率、墙角竖直、顶面和底面方向一致；几何投影应精确一致，栅格检查允许抗锯齿约 1px 的读数误差，不能借此接受系统性的偏斜。
- [ ] 人物、门洞、楼层、树木、城墙、路灯和长椅在同一实机画面下尺度合理。
- [ ] 四向建筑结构对应；八向人物动作完整、持物一致、脚底稳定。
- [ ] 左上光源、右下投影、阴影软硬、材质粗糙度与整体明亮度一致，没有双重阴影。
- [ ] 屋顶颜色、建筑轮廓、树种和树形有受控变化，材质略旧且细节密度统一。
- [ ] 门墙、塔楼、桥与墙角连接无缝；路沿转弯、绿地边缘和人行道连续，没有悬空或缺面。
- [ ] 所有正式精灵透明正确，原点、裁切、图集边缘和过滤在实际游戏倍率与允许缩放范围内正常。
- [ ] 游戏真正加载新素材；入口、传送落点和任务路线可达，碰撞不穿地基，前后排序正确，遮住角色的环境素材保持不透明。
- [ ] 相关检查与真实引擎截图已保存；报告明确哪些已完成、哪些仍是过渡素材。文档更新本身不算素材或集成完成。

环境素材替换后，按改动范围运行城市通行/遮挡检查及 `make forest-test`；角色改动补做八向逐帧和实际移动检查。纯文档修改检查内容与链接即可，不需要为此重新运行游戏测试。
