# 一级冰音效来源修正 · v8

用户报告冰音效不对后，检查发现 v6 使用的 `cgefc15.wav` 与先前火焰参考完全相同，源 SHA256 为 `50e492…b7e88`。下载目录另有独立的 `冰1.wav`，本版据文件命名选择它作为替换并交付试听；不将这次选择描述为用户已完成音色验收。

- 原文件：`/Users/dev/Downloads/冰1.wav`，约 0.791701 秒，22.05 kHz 单声道。
- 原样保存的工作副本：`art/audio/ice-v8/user-supplied/ice-level-1-source.wav`。
- 运行时：`hit-level-1.wav`，48 kHz 双声道 PCM，降低 2 dB，末尾 12 ms 防截断淡出；保留原音高和节奏。
- 生成脚本：`tools/prepare_ice_hit.py`。存在指定输入时刷新工作副本，避免原目录已有旧文件导致继续复用错误录音。
- 来源及文件哈希：`manifest.json`。此为用户电脑中的录音，不标记为原创合成或 CC0。

保持既有冰魔法时序：每根柱从自己的释放时刻开始生长，1.15 秒长到最高时才播放声音并结算伤害/冻结；多重柱在释放窗口内依次出现，8 帧、固定落点、回缩和淡出不变。

`tools/ice_review.gd` 除检查实际峰顶触发外，还核对录音名称、源文件及运行时 SHA，并检查源文件与火焰参考不同，防止此次错误重复。

实机试听：`preview/ice-v8.mp4`；事件：`preview/ice-v8-events.json`。v6 的文件和影片保留为历史版本。
